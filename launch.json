{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-node",
            "request": "launch",
            "name": "parser debug",
            "program": "${workspaceFolder}/dist/parser.js",
            "outputCapture": "std"
        }
    ]
}